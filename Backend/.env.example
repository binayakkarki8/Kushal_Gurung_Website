# ==================== ENVIRONMENT VARIABLES ====================
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=production

# Email Configuration (Gmail Example)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# For Gmail:
# 1. Go to Google Account Settings
# 2. Security → 2-Step Verification (enable it)
# 3. Security → App Passwords
# 4. Generate app password for "Mail"
# 5. Use that 16-character password here

# Admin Email (where form submissions are sent)
ADMIN_EMAIL=contact@kushalgurung.com

# Database Configuration (Optional - if using MongoDB)
# MONGODB_URI=mongodb://localhost:27017/kushal-portfolio
# Or for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kushal-portfolio

# Frontend URL (for CORS)
FRONTEND_URL=https://kushalgurung.com

# Security
# JWT_SECRET=your-secret-key-here (if implementing authentication)

# API Keys (if needed)
# SMS_API_KEY=your-sms-api-key (if sending SMS notifications)
# RECAPTCHA_SECRET=your-recaptcha-secret (if using Google reCAPTCHA)